Step 2 / final step of snobol4-expr-cleanup. Added leading unary minus by synthesising an OE_INT(0) slot before the primary-token loop and queuing TK_MINUS in PEND_OP, leveraging step 1's deferred-marker mechanism. -X compiles to the slot stream [0, X-slots, OE_OP_SUB] which the postfix walk evaluates as 0-X = -X. Seven new unary-minus cases pass; three regression cases unchanged. examples/negative_output.sno extended to include the OUTPUT = -5 literal that was previously documented as out-of-scope. just demos + just test green. Saga snobol4-expr-cleanup complete: D (negative-int literal output) and E (multi-op chains) both shipped.