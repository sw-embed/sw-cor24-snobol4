Step 1 of snobol4-expr-cleanup. Extended the in-stream OE_OP_* marker scheme (introduced by saga-expr-completeness step 3 for builtin args) to the outer assignment-context expression parser, fixing multi-op chains. PEND_OP local in sno_lex's assignment loop defers the marker emission until after the second operand parses, producing slot streams like [A, B, OE_OP_ADD, C, OE_OP_ADD] for A + B + C. sno_exec's OE_BINOP path drops the trailing SELECT-on-S_OEOP that was redundant for single-op statements and wrong for chains. Six previously-shipped regression cases stay green; five new multi-op cases produce correct left-to-right results including the previously-out-of-scope (SIZE(A)-1)+1.