Added descriptor dumper (DUMP_DESC), heap header dumper (DUMP_HDR), heap walker (HEAP_WALK), and heap consistency checker (HEAP_CHECK) to snolib.plsw. Output format: DESC @ADDR tag=name sub=HH pay=PPPPPP and OBJ @ADDR kind=name size=SSSSSS fl=HH. Walker iterates contiguous blocks. Checker validates size, kind, and overflow. 4 tests passing. Filed plsw#12 (.comm overlap with .data).