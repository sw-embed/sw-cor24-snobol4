Originally underscore-label only; expanded scope after dcftn filed three regression briefs (rawinput-eof-garbage, rawinput-mid-file-halt, concat-after-funcall-truncates) from the prior cap-saga merge. All four bugs fixed in one commit (63d20fb): IS_AN accepts '_' (1 line); SRC_LOAD_ADDR/INP_LOAD_ADDR moved 0x80000->0xE0000 / 0x90000->0xF0000 to avoid the binary-grew-past-load-region collision (fixes both rawinput bugs); HAS_BLT lowering rewritten as unified left-to-right marker-aware walker (fixes single AND multi-builtin in concat). Plus substantial coverage uplift: per-demo .expected fixtures + scripts/check-demo.sh (just demos now diffs output, not just halt-status), scripts/test-rawinput-matrix.sh (exhaustive RAWINPUT line-count coverage), examples/fti_smoke.sno (dcftn workload miniature), build-time binary-size assertion. 23/23 demos green; 14 unit + 20 matrix tests green.