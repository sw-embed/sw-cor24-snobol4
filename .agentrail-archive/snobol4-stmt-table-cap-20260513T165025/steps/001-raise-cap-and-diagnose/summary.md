Raised STMAX 256 -> 1024 in include/snoglob.msw (EPMAX 2048 -> 8192 to match) and added a compile-time overflow diagnostic in PARSE that sets SB_OVERFLOW + emits STMT_OVF_MSG, so AM_EXEC's existing overflow gate halts cleanly instead of executing a corrupt program. Pre-fix: 240-stmt program emitted 85031 garbage lines (control wrapped to PC=0 due to missing END marker). Post-fix: 244 correct lines. Added examples/many_outputs.sno (280-stmt regression). just demos green (all 16), just test green (14 All tests done). Build artefact rebuilt 168 KB -> 282 KB.