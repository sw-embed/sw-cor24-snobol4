Bumped PSTK_DEPTH 16 -> 32 + EPSLOTS 16 -> 32 (EPMAX 16384 -> 32768). PSTK was the deeper cause: the runtime pattern stack was hardcoded 16-deep, so even after step 3's EPSLOTS bump the matcher overflowed at ~5-6 captures with separators (corrupting adjacent state, producing bizarre wrong captures). 6-capture brief target now binds correctly. Added EP_OVF_MSG + post-PP-loop overflow diagnostic in PARSE: 12+ capture stretch patterns trip the diagnostic + SB_OVERFLOW for clean halt instead of silent wrong bindings. examples/pattern_captures.sno covers 1..6 captures. just demos 20/20 green; just test 14/14 green. Binary 434 KB -> 631 KB (still under 1 MB SRAM).