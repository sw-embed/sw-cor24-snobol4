Implemented ANY(class) pattern primitive end-to-end: new OE_ANY (snoglob.msw), new OP_MAKE_ANY (am.msw), parser recognition in pattern context (sno_lex.plsw mirroring SPAN), lowering of OE_ANY to OP_MAKE_ANY in both match and replace contexts, EXEC_PAT_BUILD case that pushes PK_ANY onto the pattern stack, PMATCH cases in match+replace using a new EXEC_ANY helper (IN_CLASS membership test). Added examples/any_pattern.sno with 7 tests (single/multi-char class, no-match, empty subject, capture). Trimmed 3 verbose comment banners in sno_exec.plsw to stay under pl-sw's 64 KiB SRC_BUF cap (added code pushed it from 64640 to ~65500, comment trim freed ~580 bytes). just demos 18/18 green; just test 14/14 green. build/snobol4.bin 335 KB -> 336 KB.