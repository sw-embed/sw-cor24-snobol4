Integrated AM pipeline into snobol4.plsw. Full flow: READ_SRC -> PARSE -> LOWER_ALL -> AM_EXEC. Lowering translates statement table to AM bytecode with OUTPUT detection (FIND_OUTPUT) and goto fixup (FIXUP_GOTOS two-pass). AM executor runs fetch-decode-execute with eval stack and variable array. All 3 existing examples produce identical output. Milestone 2 complete -- parser and executor separated by explicit AM boundary.