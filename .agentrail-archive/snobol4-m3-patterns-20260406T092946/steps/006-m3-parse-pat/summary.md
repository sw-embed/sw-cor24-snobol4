Full pattern matching integration. Parser handles SPAN('...') and SUBJECT PATTERN . CAPTURE :F(). AM opcodes: MAKE_SPAN, PAT_MATCH, PRINT_VAR. Pattern engine: IN_CLASS, EXEC_SPAN, unanchored scan, capture. VTYP tracks variable types. LAST_RESULT drives :S/:F. SPAN example runs: 'abc 123 xyz' -> captures '123'. Failure path works: 'abc xyz' -> 'no match'. Filed plsw#23 and #24 (both fixed).