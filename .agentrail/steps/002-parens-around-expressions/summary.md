Added parens support in the assignment-context expression parser. TK_LPAREN at primary position is consumed silently; trailing TK_RPAREN at end-of-iteration is consumed via a DO WHILE; a second binary-op check after the close-parens handles post-paren arithmetic. examples/parens_expr.sno covers 6 forms; just demos / just test green. Multi-op chain limitation documented as separate (S_OEOP is single-valued; pre-existing). sno_lex.s grew ~1.8 KB, well within headroom.