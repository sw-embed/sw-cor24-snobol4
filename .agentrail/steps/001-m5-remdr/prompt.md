Add REMDR(A,B) as a numeric 2-arg expression builtin that returns A mod B on the eval stack. Model the parser recognition on how SPAN/BREAK are detected inside assign expressions (sno_lex.plsw). Add an OP_MOD opcode (raw value 35 — skip new %DEFINE to stay under the compiler's define-table limit). Lowering emits LOAD A, LOAD B, OP_MOD. Executor pops two ints and pushes (a mod b) via repeated subtraction (watch for a=0 or b=0 edge cases). Update FIXUP_GOTOS to recognize OP_MOD as zero-operand. Add a tiny test that assigns R = REMDR(7,3) and outputs R (should print 1). Keep source under the 256-byte SRC buffer. Commit when green.