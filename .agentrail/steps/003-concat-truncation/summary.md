Bumped EPSLOTS 8 -> 16 (EPMAX 8192 -> 16384) in snoglob.msw to fix concat operand silent-truncation past 8 slots. The brief's repro 'a' NL 'b' NL 'c' NL 'd' NL 'e' now produces SIZE 9 (was 8). Added examples/concat_chain.sno with 7 tests covering 2..13 operand concats including the brief's exact repro and the 2-byte discriminator. While writing tests, surfaced an UNRELATED latent bug where OUTPUT lines that interleave a builtin with trailing operands drop the trail -- documented in the commit, not in scope for this brief. Binary 336 KB -> 434 KB (4x EPMAX-sized INT arrays). just demos 19/19 green; just test 14/14 green.