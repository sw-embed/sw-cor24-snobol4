Write examples/fizzbuzz.sno that prints classic FizzBuzz for N=1..15 on the modular interpreter, using: a loop via :(LOOP), LE/GT builtin to stop, REMDR for divisibility, IDENT or EQ to branch, and OUTPUT concatenation. Each iteration prints either 'Fizz', 'Buzz', 'FizzBuzz', or the number itself. Keep under 256-byte SRC and 16-statement STMAX — be terse (short labels, minimal whitespace). Verify the expected 15 lines are produced. Commit when green.