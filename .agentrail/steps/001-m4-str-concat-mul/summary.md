Added multiplication (TK_STAR, OP_MUL, 7*6=42) and string concatenation (expression parts model with up to 4 parts, OP_CONCAT joins strings in SB). Parser collects adjacent values as expression parts. Lowering emits LOAD+CONCAT chains. OUTPUT uses PRINT_VAR for runtime type checking. New examples: multiply.sno (42), concat.sno ('Hello, ' NAME '!' -> Hello, World!). All existing examples verified. Filed plsw#26 (source buffer, fixed).