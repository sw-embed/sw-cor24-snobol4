Extracted src/snolib.plsw runtime library (DESC_*, HDR_*, SLEN/SCEQ/IN_CLASS, HEAP_INIT/ALLOC/FREE). Migrated test_snolib + test_snolib2 to %INCLUDE snolib instead of inline copies. Updated scripts/build.sh to register .plsw library files for %INCLUDE; unblocked src/snolib.plsw in .gitignore. Modular interpreter build unchanged (snolib not yet wired in -- that's step 2). All snolib tests pass; full just test passes; just rebuild byte-identical.