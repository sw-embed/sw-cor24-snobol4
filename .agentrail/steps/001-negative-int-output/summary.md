Fixed EMIT_DEC and PUT_DEC to handle negative integers. Both PROCs now detect N<0, emit '-', negate, and fall through to the existing positive-decimal loop. examples/negative_output.sno regression demo covers 6 computed-negative cases plus zero/positive baseline. just demos + just test green. The literal-negative parser case (OUTPUT = -5) is documented as out-of-scope -- it's a separate parser limitation that needs TK_MINUS as a leading primary token.