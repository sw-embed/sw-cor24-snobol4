Step 3 / final step of saga-expr-completeness. Added OE_OP_ADD/SUB/MUL/MOD inline binop markers to snoglob.msw; new PARSE_BUILTIN_ARG helper in sno_lex.plsw that parses primitive (or nested call via PARSE_NESTED_FN) + optional binop + second primary; wired into SUBSTR/SIZE/CHAR arg parsers. sno_exec.plsw concat-path BLT walk and OE_BINOP path now dispatch on EP_TYP via SELECT, handling OE_SIZE/SUBSTR/CHAR markers and the new OE_OP_* markers mid-stream. All three dcftn-blocking patterns now work: SUBSTR(A,SIZE(A),1) -> 'O', SIZE(SUBSTR(A,1,3)) -> 3, SUBSTR(A,1,SIZE(A)-1) -> 'HELL'. sno_exec.s grew past the old 262144 EMIT_BUF cliff (now 266209 bytes) without truncation -- streaming-emit working as intended. examples/builtin_arg_expr.sno regression demo wired in; just demos / just test green; no regression in prior saga steps.